[project]
name = "arxiv-search"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "arxiv",
    "faiss-cpu",
    "pandas",
    "pydantic[email]",
    "pytest",
    "pyyaml",
    "rank-bm25",
    "sentence-transformers",
    "streamlit",
    "tqdm",
]
